name: AutoCompiler
on:
  push:
    branches: [ "main" ]

permissions:
  contents: write

jobs:
  CompileAndRelease:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - run: sudo apt-get update && sudo apt-get install -y libncurses5-dev libncursesw5-dev
      
      - run: gcc main.c -o 2duniverse -lncurses -lm
      
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create v1.0.${{ github.run_number }} 2duniverse --title "v1.0.${{ github.run_number }}" --notes "Bot released"
